#Example code #to use openai in chatgpt to generate a picture this can be used by people to deside what they wish there plant to look like on their tamagotch

import openai

# Define the prompt for DALL-E to create a Tamagotchi style plant
prompt = "A cute, animated plant in a Tamagotchi style, with a small pot, bright colors, and expressive eyes."

# Generate the image using OpenAI's DALL-E model
response = openai.Image.create(
    prompt=prompt,
    n=1,
    size="512x512"
)

# Get the image URL
image_url = response['data'][0]['url']
image_url
